import { defineComponent } from 'vue';

export default defineComponent({
  name: 'QASManage',
  setup() {
    return () => {
      return (
        <div>
          <div>manage</div>
        </div>
      );
    };
  },
});
