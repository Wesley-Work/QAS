import { defineComponent } from 'vue';

export default defineComponent({
  name: 'Manage',
  setup() {
    return () => {
      return (
        <div>
          <div>This is Manage Page</div>
        </div>
      );
    };
  },
});
